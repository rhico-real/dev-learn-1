# Build Log: Apr 27, 2026
**Project:** RunHop — a social + event platform for running races
**Stack:** NestJS · TypeScript · PostgreSQL · Prisma · Jest
**Tasks:** Phase 3 Payments finish pass, registration/payment integration, docs notes

---

## What I Did

Wrapped the payment flow into the rest of the event domain instead of leaving it as an isolated CRUD module. The main work was wiring review actions back into registrations, tightening controller access checks, and adding broader test coverage around the payment lifecycle.

- Extended `PaymentController` in `src/domain/event/payment/payment.controller.ts` (143 lines) with:
  - `GET /events/:eventId/payments` for org admins or super admins
  - `PATCH /payments/:id/review` for approve / reject review actions
  - optional pagination query handling on `listByRegistration`
- Updated `PaymentModule` to import `RegistrationModule`, `RaceModule`, `OrganizationContextModule`, and `EventModule` so the controller could verify ownership and org-level permissions without collapsing everything into one service.
- Updated `PaymentService` in `src/domain/event/payment/payment.service.ts` (251 lines):
  - reads `MAX_PAYMENT_ATTEMPTS` from `ConfigService` instead of a hardcoded constant
  - returns a more specific message when a registration is no longer `PENDING`
  - keeps the event-driven review flow: approve emits `payment.approved`, reject emits `payment.rejected`
- Extended `RegistrationController` with `GET /registrations/:id` so the payment review flow can resolve the owning race and org from a registration.
- Updated `RegistrationService` in `src/domain/event/registration/registration.service.ts` (252 lines):
  - free races (`price === 0`) now create registrations directly as `CONFIRMED`
  - listens for `payment.approved` and flips the registration to `CONFIRMED`
  - listens for `payment.rejected` and cancels the registration after the configured max rejection count
  - relies on Prisma `P2002` handling for duplicate registration conflicts instead of pretending a pre-check is enough
- Extended unit coverage:
  - `payment.service.spec.ts` now injects `ConfigService`
  - `registration.service.spec.ts` adds free-race auto-confirm coverage and duplicate-registration conflict coverage
- Expanded e2e coverage:
  - `test/e2e/payment.e2e-spec.ts` (443 lines) covers submit, approve, ownership checks, amount validation, active-payment conflict, and review authorization
  - `test/e2e/registration.e2e-spec.ts` (427 lines) now covers free-race auto-confirm and `GET /registrations/:id`
- Added two notes to `docs/guides/additional-notes.md`:
  - a short `readonly` rule-of-thumb section
  - a “How To Read Unfamiliar NestJS Syntax” section using `@OnEvent(...)` and typed payload params as the example

---

## Struggles

**The unique constraint story was easy to get wrong if I stayed at the service layer.** The tempting version is “check if the row exists, then create.” That reads nicely, but it breaks down under concurrency. The actual enforcement has to stay in the database with `@@unique([userId, raceId])`, and the service needs to translate Prisma `P2002` into a `409 Conflict`.

**Compound unique syntax vs normal filters.** `findUnique` does not accept `where: { userId, raceId }` even when the pair is declared with `@@unique`. It needs the generated compound key shape: `userId_raceId: { userId, raceId }`. That was one of those Prisma rules that makes sense once you separate “general filtering” from “lookup by declared unique key.”

**Event-driven side effects needed a clean owner.** Approving a payment should confirm a registration, but that state change belongs to the registration context, not the payment service. The clean split was: payment emits, registration listens. That kept the payment review logic short and let the registration module own its own state transitions.

---

## Wins

- The payment flow now feels like part of the product instead of a partially wired module. Reviewing a payment actually drives the registration lifecycle.
- Moving `MAX_PAYMENT_ATTEMPTS` into config cleaned up a magic number and made the rejection-cancel behavior environment-driven.
- Free races are simpler now: registration creation itself decides whether the user should start in `PENDING` or `CONFIRMED`.
- The docs note addition was worth doing. I kept running into the same teaching moments around `readonly`, decorators, and typed function params, so capturing them once in `additional-notes.md` will save repeated explanation later.

---

## Next Up

- Bring up the e2e test database and run the new payment and registration e2e specs end-to-end.
- Add payment list filtering tests around `GET /events/:eventId/payments`, especially status filtering and org admin authorization edges.
- Clean up any remaining rough edges in the payment review permission path, especially the registration → race → event lookup chain.

---

## STAR Story (Apple Behavioral)

**Situation:** I had the payment module mostly built, but it still behaved like an isolated subsystem. Approving a payment changed the payment row, but the registration lifecycle was not fully integrated around it, and the surrounding controller paths were still thin.

**Task:** Finish the payment flow so it behaves correctly at the product level: free races auto-confirm, paid races confirm only after approval, repeated rejections can cancel the registration, and admins can review and list payments through org-scoped endpoints.

**Action:** I kept the review logic in `PaymentService`, but pushed registration state changes behind event listeners in `RegistrationService` using `@OnEvent('payment.approved')` and `@OnEvent('payment.rejected')`. I moved the max rejection count behind `ConfigService` so it comes from env, added `GET /registrations/:id` to support the review permission path, and extended both unit and e2e tests around the free-race and review flows. I also leaned on Prisma’s `P2002` handling instead of trusting pre-checks for duplicate registration protection.

**Result:** The flow is now coherent end-to-end at the service design level: free races confirm immediately, paid races confirm on approval, repeated rejections can cancel the registration, and the admin review endpoints are wired into the existing org permission model. Build and targeted unit verification passed, and the only remaining blocker on full e2e verification is the local test database not running on `localhost:5433`.

---

## Social Posts

### X
```text
Day 36 of RunHop: finished the payment flow instead of leaving it as isolated CRUD.

Shipped:
→ payment review endpoint for admins
→ event-scoped payment listing
→ free race registrations auto-confirm
→ payment.approved / payment.rejected listeners in registration
→ MAX_PAYMENT_ATTEMPTS from config instead of a magic number

The useful distinction today: pre-checks are convenience, DB constraints are enforcement.
```

### LinkedIn
```text
Day 36 building RunHop — a social + event platform for running races.

Today I finished the integration pass on the payments module so it behaves like part of the event system, not a standalone CRUD feature.

What changed:
- Added admin review on `PATCH /payments/:id/review`
- Added event-scoped payment listing on `GET /events/:eventId/payments`
- Free races now create registrations directly as `CONFIRMED`
- Payment approval emits an event that confirms the registration
- Repeated payment rejection can cancel the registration after the configured max attempt count
- Moved `MAX_PAYMENT_ATTEMPTS` behind `ConfigService` so it comes from env instead of a hardcoded constant
- Expanded unit and e2e coverage around payment submission, approval, and registration state transitions

The most useful design decision today was keeping registration state changes in the registration context. The payment service emits events, but the registration service owns the transition to `CONFIRMED` or `CANCELLED`. That kept the dependency graph cleaner and made the lifecycle easier to reason about.

Also had a good reminder from Prisma: `P2002` on create is the real duplicate-registration safeguard. Pre-checks are helpful for readability, but the database constraint is still the source of truth.

Next step is straightforward: bring up the e2e database and run the new payment specs all the way through.

#NestJS #TypeScript #Prisma #BackendDevelopment #SoftwareArchitecture
```

### dev.to
```text
---
title: "Finishing a Payments Flow in NestJS: Events, Config, and the Limits of Pre-Checks"
tags: nestjs, typescript, architecture, learninpublic
---

I'm building RunHop in public, a social + event platform for running races. Today was a cleanup-and-integration day for the payments module.

The core problem wasn't creating payments. That part already existed. The real work was making payment review actually affect the rest of the system in a clean way.

## The Product-Level Rule

There are two different registration paths:

- Free race: registration should be confirmed immediately
- Paid race: registration should stay pending until payment review is approved

That sounds simple, but it creates a design question: when a payment is approved, which service owns the registration state change?

## Keep the State Transition with the Owning Context

My first instinct was the obvious one: if `PaymentService.review()` approves the payment, just update the registration there too.

That would work, but it would make the payment module responsible for registration lifecycle rules. I didn't want that coupling.

So I kept the payment review logic in `PaymentService` and emitted domain events:

```ts
this.eventEmitter.emit(NotificationEventTypes.PAYMENT_APPROVED, {
    paymentId: payment.id,
    registrationId: payment.registration.id,
});
```

and

```ts
this.eventEmitter.emit(NotificationEventTypes.PAYMENT_REJECTED, {
    paymentId: payment.id,
    registrationId: payment.registration.id,
    rejectionCount: rejectionCount,
});
```

Then `RegistrationService` listens:

```ts
@OnEvent(NotificationEventTypes.PAYMENT_APPROVED)
async handlePaymentApprovedEvent(event: {
    paymentId: string;
    registrationId: string;
}) {
    await this.prisma.registration.update({
        where: { id: event.registrationId },
        data: { status: 'CONFIRMED' },
    });
}
```

and:

```ts
@OnEvent(NotificationEventTypes.PAYMENT_REJECTED)
async handlePaymentRejectedEvent(event: {
    paymentId: string;
    registrationId: string;
    rejectionCount: number;
}) {
    if (
        event.rejectionCount >=
        this.configService.get<number>('MAX_PAYMENT_ATTEMPTS', 3)
    ) {
        await this.prisma.registration.update({
            where: { id: event.registrationId },
            data: { status: 'CANCELLED' },
        });
    }
}
```

That split turned out cleaner than I expected. Payment owns payment review. Registration owns registration status.

## Free Races Should Skip the Pending State

I also adjusted registration creation to handle free races directly:

```ts
return await this.prisma.registration.create({
    data: {
        userId,
        raceId,
        ...(race.price === 0
            ? { status: RegistrationStatus.CONFIRMED }
            : {}),
    },
});
```

This removed the need for a separate confirmation step for zero-price races. The service can decide the initial state up front from the race price.

## `P2002` Is the Real Duplicate Guard

One of the more useful reminders today was about duplicate registrations.

At the application level, it's tempting to do:

1. check if the registration exists
2. if not, create it

That works in the happy path, but it's not enough under concurrency. Two requests can pass the pre-check before either insert commits.

The actual protection comes from the Prisma schema:

```prisma
@@unique([userId, raceId])
```

and the create call needs to treat `P2002` as the authoritative duplicate signal:

```ts
} catch (error) {
    if (
        error instanceof Prisma.PrismaClientKnownRequestError &&
        error.code === 'P2002'
    ) {
        throw new ConflictException('You are already registered for this race.');
    }

    throw error;
}
```

That changed how I think about the flow.

- Pre-checks improve messaging and readability
- Unique constraints enforce reality

They're not interchangeable.

## Config Instead of Magic Numbers

The payment module originally had:

```ts
const MAX_PAYMENT_ATTEMPTS = 3;
```

I moved that behind `ConfigService` so the service reads from env instead:

```ts
this.configService.get<number>('MAX_PAYMENT_ATTEMPTS', 3)
```

Small change, but it matters. Retry limits are a policy decision, and policy is usually config, not hardcoded behavior.

## Endpoint Work

I also finished the controller side so the flow is reachable in the app:

- `GET /events/:eventId/payments`
- `PATCH /payments/:id/review`
- `GET /registrations/:id`

The review path uses existing org membership checks so admins can review event payments without opening up the endpoint to everyone.

## Verification

Fresh verification for the session:

- `npm run build` passed
- targeted unit tests passed: 4 suites, 57 tests
- e2e is partially blocked right now because the local test database on `localhost:5433` was not running during the session

That last part matters. The code is in much better shape, but I don't want to blur “designed and unit-tested” with “fully e2e-verified.”

## Takeaway

The useful lesson from today wasn't “how to add a payment review endpoint.” It was that a flow starts feeling solid when the owning modules each handle their own state transitions.

Payment review should not secretly become registration business logic.

Events were the clean line here.
```

### Substack
```text
Subject: I finally stopped treating payments like isolated CRUD

Today I finished the integration pass on the payments flow in RunHop.

The key shift was simple: approving a payment shouldn't just update the payment row. It should drive the rest of the lifecycle. For paid races, that means confirming the registration. For repeated failed submissions, it can mean cancelling the registration after a configured number of attempts.

The clean version ended up being event-driven. Payment review emits `payment.approved` or `payment.rejected`, and the registration service listens and owns the status change. That kept the boundary cleaner than stuffing everything into the payment service.

I also moved `MAX_PAYMENT_ATTEMPTS` into config, which is one of those small changes that makes the code feel less temporary.

Build and targeted unit tests passed. The only thing I didn't get through cleanly was e2e, because the local test DB wasn't up on `localhost:5433`.

Talk soon, Rhico
```
