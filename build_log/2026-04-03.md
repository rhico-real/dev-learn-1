# Build Log: Apr 3, 2026
**Project:** RunHop — a social + event platform for running races  
**Stack:** NestJS · TypeScript · PostgreSQL · Prisma · Redis · Jest · Docker  
**Task:** 8 — Event Context (Race module polish + correctness)

---

## What I Did

Focused on tightening Task 8 and validating the Race module end-to-end. This was a polish pass, but it fixed a couple of correctness holes.

- Fixed `RaceService.findById()` to throw `NotFoundException` when a race doesn’t exist, instead of returning `null` and causing downstream `race!.eventId` crashes. File: `src/domain/event/race/race.service.ts`.
- Cleaned unused imports in the race module (`rxjs` had slipped in). File: `src/domain/event/race/race.service.ts`.
- Corrected a false-positive e2e test: the “update on published event” test was calling `POST /events/:id/races` (create) instead of `PATCH /races/:id`. Now it correctly asserts that updates are blocked after publish. File: `test/e2e/race.e2e-spec.ts`.

This is the kind of pass that doesn’t add features, but it removes bad behavior and makes the test suite reflect reality.

---

## Struggles

**Pagination tests tied to global data.** The org pagination test uses a fixed `limit` derived from how many orgs existed across all tests. That makes the test brittle: add an org in a different suite and the “last cursor” logic breaks. The fix is to make the test independent of global counts (page until empty, or filter to a known prefix). I haven’t updated the test yet, but it’s now on my list to harden.

---

## Wins

- Race mutations now return clean 404s on missing race IDs instead of 500s from `race!.eventId`.
- The “update on published event” e2e test now actually covers updates, not accidental create calls. One less false green.
- Task 8 feels stable enough to move on to Task 9.

---

## Next Up

- Task 9 — Registration module (register, capacity check, unique constraint handling).
- Optional: harden the org pagination e2e test to remove its dependency on global org count.

---

## STAR Story (Apple Behavioral)

**Situation:** The Race module’s e2e test suite reported green, but the “update on published event” test was actually calling the create endpoint. This meant a key behavior (blocking updates after publish) wasn’t really tested.

**Task:** Make the test assert the correct behavior and eliminate the false positive.

**Action:** I inspected the e2e spec and found the test using `POST /events/:id/races` instead of `PATCH /races/:id`. I switched it to `PATCH`, targeted an existing race (`raceTest2.id`), and kept the expected 400 status. I also cleaned up `RaceService.findById()` to throw `NotFoundException`, so the rest of the service can safely assume a race exists without blowing up.

**Result:** The suite now tests real update semantics on published events, and missing races now respond with 404 instead of 500. The tests match the real product behavior instead of giving me false confidence.

---

## Social Posts

### X (Twitter)
```
day 15 of building RunHop

today: cleaned up Task 8 (Race module)
→ fixed RaceService.findById to throw NotFoundException (no more null → 500)
→ corrected an e2e test that was “updating” via POST

passing tests are useless if they test the wrong endpoint. small fixes, big signal.
```

---

### LinkedIn
```
Day 15 building RunHop — a social + event platform for running races on NestJS.

Today was a polish pass on the Race module (Task 8). No new features, but I fixed behavior gaps and tightened tests.

What I did:
• Added a proper NotFoundException in RaceService.findById() so missing races return 404 instead of a 500
• Removed unused imports in the race module
• Fixed a false-positive e2e test: the “update on published event” case was calling POST (create) instead of PATCH (update)

The interesting part: a green test suite can still lie. If the endpoint is wrong, you’re testing the wrong behavior. It’s a good reminder to read the HTTP calls and not just the assertions.

Next: Task 9 — registration module (capacity checks + unique constraint handling).

#NestJS #TypeScript #Testing #SoftwareArchitecture #Backend #LearningInPublic
```

---

### dev.to
```
---
title: "When a Passing Test Lies: Fixing an E2E False Positive in NestJS"
tags: nestjs, typescript, architecture, learninpublic
---

I’m building RunHop in public — a social + event platform for running races, built on NestJS. This is day 15.

Today wasn’t a feature day. It was a correctness day. I found a test that passed while testing the wrong behavior, and fixed a quiet 500 hiding behind a non-null assertion. Both are easy to miss, and both are exactly how bugs slip into production.

## The Bug: An E2E Test That Didn’t Test Updates

In the Race module, I enforce a rule: once an event is published, races under it can’t be created, updated, or deleted.

I had an e2e test named:

```
it('should be 400 if updating a race on a published event', async () => {
  // draftEvent here is now published because of beforeAll
  const res = await request(app.getHttpServer())
    .post(`/api/v1/events/${draftEvent.id}/races`)
    .set('Authorization', `Bearer ${accessToken}`)
    .send({
      name: 'Race Test 2',
      distance: 123.45,
      unit: 'test-unit-2',
      maxParticipants: 500,
      price: 1000
    });

  expect(res.statusCode).toBe(400);
});
```

The problem is obvious once you stare at it: this test is calling `POST /events/:id/races` — which is the *create* endpoint. It wasn’t testing updates at all. The test passed, but for the wrong reason.

This is the dangerous kind of bug: it gives you confidence you didn’t earn.

### The Fix

I changed it to call the correct update endpoint:

```typescript
it('should be 400 if updating a race on a published event', async () => {
  const res = await request(app.getHttpServer())
    .patch(`/api/v1/races/${raceTest2.id}`)
    .set('Authorization', `Bearer ${accessToken}`)
    .send({ name: 'Updated Race Test' });

  expect(res.statusCode).toBe(400);
});
```

That’s it. Same assertion, correct endpoint. Now the test actually verifies the rule I care about.

## The Other Bug: Non-Null Assertions Hiding 500s

In `RaceService`, I had:

```typescript
async findById(id: string) {
  return this.prismaService.race.findUnique({ where: { id } });
}
```

Every caller did this:

```typescript
const race = await this.findById(id);
await this.verifyEventIsDraft(race!.eventId);
```

If a race didn’t exist, `findUnique()` returned `null`. The non-null assertion (`race!`) then caused a runtime error when accessing `race!.eventId`. That’s a 500, and it’s the worst kind: a missing record should be a 404, not an internal crash.

### The Fix

I moved the not-found handling into `findById()`:

```typescript
async findById(id: string) {
  const result = await this.prismaService.race.findUnique({ where: { id } });
  if (!result) throw new NotFoundException('Race not found');
  return result;
}
```

Now every caller gets a consistent 404 for missing races, and I can remove defensive checks everywhere else.

## Why This Matters

These changes are small, but they keep the system honest:

- The e2e tests now prove what they say they prove.
- The service layer returns correct HTTP errors instead of crashing.

It’s easy to chase new features and ignore this kind of cleanup. But if you’re building in public (or building at all), correctness work is what keeps the platform stable as it grows.

## Takeaway

Passing tests don’t mean correct behavior. Look at the HTTP calls. Make sure the tests are hitting the right routes. And when you see `!` on a nullable value, treat it as a warning — it’s often hiding a missing `NotFoundException`.

Small fixes, big signal.
```

---

### Substack
**Subject:** A passing test that wasn’t testing anything

Today I cleaned up Task 8 in RunHop (the Race module), and the most interesting part wasn’t a feature — it was a false green test.

I had an e2e test labeled “update on published event,” but it was calling the create endpoint. The test passed, but it wasn’t proving what it said it was proving. That’s the kind of bug that feels harmless until you ship something relying on it.

I fixed the endpoint to `PATCH /races/:id` and kept the same 400 assertion. At the same time, I tightened `RaceService.findById()` so missing races throw a 404 instead of crashing with `race!.eventId`.

The common theme: small correctness fixes reduce long‑term risk more than another shiny feature. Passing tests are only useful if they’re testing the right thing.

Talk soon, Rhico
