# Build Log: Mar 28, 2026
**Project:** RunHop — a social + event platform for running races
**Stack:** NestJS · TypeScript · PostgreSQL · Prisma · Redis · Jest · Colima
**Task:** 6 — Organization Context (e2e tests + infrastructure)

---

## What I Did

Spent the day on e2e test infrastructure and the organization e2e test suite.

Set up a dedicated test database (`runhop_test`) so e2e tests stop touching dev data. Built the pipeline:
- `test/e2e-setup.ts`: Jest `globalSetup` — loads `.env.test`, runs `prisma migrate reset --force` against `runhop_test` before every run
- `test/e2e-env-setup.ts`: Jest `setupFiles` — loads `.env.test` into the test worker process so `ConfigModule` connects to the right database
- Updated `jest-e2e.json`: `globalSetup`, `setupFiles`, fixed `testRegex` to catch files in `test/e2e/`, added `transformIgnorePatterns` for the `uuid` ESM issue

Migrated from Docker Desktop to Colima. Same Docker engine, fraction of the resources. Had to clear the `credsStore` reference in `~/.docker/config.json` after uninstalling Desktop.

Fixed all absolute `src/` imports across 11 files — converted to relative `../` paths. Added VS Code setting to prevent it from recurring.

On the org e2e test itself: register user → create org → verify OWNER membership via a separate GET to `/organizations/:id/members/find`. Caught a missing `@Body()` decorator on the org controller's `update` method — the DTO was silently `undefined`. Unit tests never caught it because they call the service directly.

Working through the remaining org e2e scenarios: update as owner, reject update as non-member, soft delete, list with pagination.

Also started reading Grokking Algorithms. Went through cursor-based pagination — how it works with Prisma's `skip: 1` + `cursor` pattern and why it beats offset pagination at scale.

---

## Struggles

**Two-process env loading.** Jest's `globalSetup` runs in a separate Node process from the test workers. Setting `process.env` in one doesn't carry to the other. Had to split the dotenv loading into two files — one for the database reset (globalSetup), one for the actual test workers (setupFiles). Not obvious until tests kept hitting the dev database.

**The `uuid` ESM problem.** Newer `uuid` ships ESM-only. Jest's default `transformIgnorePatterns` skips `node_modules`, so `uuid`'s `export` syntax blew up. Fix: `"transformIgnorePatterns": ["node_modules/(?!uuid)"]`.

**Missing `@Body()` decorator.** The org update endpoint accepted requests but the DTO was always `undefined`. Unit tests passed because they call `service.update()` directly — the decorator is a controller concern. Only the e2e test caught it. Textbook example of why both test layers matter.

---

## Wins

- Test database isolation is solid. `runhop` dev data is untouched. Every e2e run starts clean.
- The `@Body()` bug validated the whole reason for writing e2e tests — unit tests can't catch wiring issues.
- Colima migration was painless. Same `docker-compose` commands, noticeably less fan noise.
- Cursor pagination clicked after reading Grokking Algorithms alongside the Prisma implementation. Seeing the theory and the code side by side made both clearer.

---

## Next Up

- Finish remaining org e2e scenarios (update, 403 non-member, soft delete, list with pagination)
- Continue Grokking Algorithms
- Move to Task 7

---

## STAR Story (Apple Behavioral)

**Situation:** E2e tests for RunHop's organization domain were hitting the dev database. Test data was leaking into development, and there was no isolation between test runs.

**Task:** Set up a dedicated test database with automatic reset, so e2e tests run against a clean `runhop_test` database every time — without touching dev data.

**Action:** Created a two-file setup: `globalSetup` to reset the test database via `prisma migrate reset`, and a `setupFiles` script to load `.env.test` into the test worker processes. The split was necessary because Jest runs `globalSetup` in a separate Node process — env vars don't propagate to test workers. Also migrated from Docker Desktop to Colima to reduce local resource usage.

**Result:** Full test isolation. Every `npm run test:e2e` starts with a clean database, tests connect to `runhop_test` exclusively, and the dev database is never touched. The e2e tests immediately caught a missing `@Body()` decorator that unit tests had missed — proving the value of the infrastructure investment.

---

## Social Posts

### X (Twitter)
```
day 11 of building RunHop

today: e2e test infrastructure
→ dedicated test database (runhop_test) with auto-reset per run
→ migrated Docker Desktop → Colima (same engine, way less resources)
→ e2e tests caught a missing @Body() decorator that unit tests never could

unit tests prove your logic works. e2e tests prove your app works.
```

---

### LinkedIn
```
Day 11 building RunHop — a social + event platform for running races on NestJS.

Today: e2e test infrastructure and the organization test suite.

→ Dedicated test database (runhop_test) with prisma migrate reset on every run
→ Jest globalSetup + setupFiles split to handle env loading across processes
→ Migrated from Docker Desktop to Colima — same Docker engine, fraction of the resources
→ Fixed 11 files with absolute src/ imports → relative paths

The interesting problem: Jest's globalSetup runs in a separate Node process from the test workers. Setting process.env in one doesn't carry to the other. Had to split env loading into two files — one for the database reset, one for the tests themselves.

The payoff was immediate. E2e tests caught a missing @Body() decorator on the org update endpoint — the DTO was silently undefined. Unit tests never caught it because they call the service directly, bypassing the controller layer entirely.

Unit tests prove your logic works. E2e tests prove your wiring works. You need both.

Also started reading Grokking Algorithms and went deep on cursor pagination.

#NestJS #TypeScript #Testing #SoftwareArchitecture #LearningInPublic #BackendDevelopment
```

---

### dev.to
```
---
title: "Why My Unit Tests Passed But My App Was Broken"
tags: nestjs, typescript, testing, learninpublic
---

I'm building RunHop in public — a social + event platform for running races, built on NestJS. This is day 11.

Today I set up e2e test infrastructure: dedicated test database, automatic resets, proper isolation. And within the first run, the e2e tests caught a bug that unit tests had been blind to for days.

## The Bug Unit Tests Can't See

Here's the controller method that passed every unit test:

```typescript
// organization.controller.ts
@Patch(':id')
async update(
    @CurrentUser() user: interfaces.AuthenticatedUser,
    @Param('id') id: string,
    dto: UpdateOrganizationDto  // <-- spot the problem?
) {
    await this.membershipService.verifyRole(user.userId, id, 'ADMIN');
    return this.orgService.update(id, dto);
}
```

The `dto` parameter is missing `@Body()`. Without that decorator, NestJS never parses the request body into the DTO. The value is always `undefined`.

Here's the unit test that passed:

```typescript
it('should update org fields', async () => {
    mockPrisma.organization.update.mockResolvedValue({ ...mockOrg, name: 'New Name' });
    const result = await service.update('id-123', { name: 'New Name' });

    expect(mockPrisma.organization.update).toHaveBeenCalledWith({
        where: { id: 'id-123' },
        data: { name: 'New Name' }
    });
    expect(result).toEqual({ ...mockOrg, name: 'New Name' });
});
```

See why it passes? `service.update('id-123', { name: 'New Name' })` calls the service directly. No HTTP request. No controller. No decorators. The DTO is passed as a plain argument.

The e2e test caught it immediately:

```typescript
it('should be able to update org as owner', async () => {
    const org = await request(app.getHttpServer())
        .patch(`/api/v1/organizations/${orgSaved.id}`)
        .set('Authorization', `Bearer ${accessToken}`)
        .send({
            name: 'New name',
            description: 'New Description'
        });

    expect(org.statusCode).toBe(200);  // fails — dto is undefined
});
```

The fix is one word: `@Body() dto: UpdateOrganizationDto`. But the point isn't the fix. The point is that **unit tests and e2e tests catch fundamentally different categories of bugs**. Unit tests verify logic. E2e tests verify wiring.

## Setting Up a Dedicated Test Database

Before writing e2e tests, I needed test isolation. Tests hitting the dev database is a recipe for pain.

The setup: a `.env.test` file pointing to a separate `runhop_test` database:

```
# .env.test
DATABASE_URL=postgresql://runhop:runhop@localhost:5432/runhop_test
```

And a Jest `globalSetup` file that resets the database before every run:

```typescript
// test/e2e-setup.ts
import { execSync } from "child_process";
import * as dotenv from 'dotenv';

export default async function () {
    dotenv.config({
        path: '.env.test',
        override: true
    });

    execSync('npx prisma migrate reset --force', { stdio: 'inherit' });
}
```

This loads `.env.test`, then runs `prisma migrate reset --force` — which drops all tables, re-applies every migration, and gives you a clean database. The `--force` flag skips the confirmation prompt.

## The Two-Process Problem

Here's the part that wasn't obvious.

Jest's `globalSetup` runs in a **separate Node process** from the test workers. The `dotenv.config()` call in `e2e-setup.ts` sets `process.env.DATABASE_URL` in that process. When the process exits, those env vars are gone.

The test workers — where your NestJS app actually boots — never see them. So `ConfigModule.forRoot()` reads from `.env` (the dev config), and your tests hit the dev database anyway.

The fix: a second file loaded via `setupFiles`, which runs inside each test worker:

```typescript
// test/e2e-env-setup.ts
import * as dotenv from 'dotenv';

export default async function () {
    dotenv.config({
        path: '.env.test',
        override: true
    });
}
```

And the Jest config ties both together:

```json
{
  "globalSetup": "./e2e-setup.ts",
  "setupFiles": ["./e2e-env-setup.ts"],
  "testRegex": ".*\\.e2e-spec\\.ts$",
  "transformIgnorePatterns": ["node_modules/(?!uuid)"]
}
```

`globalSetup` runs once — resets the database. `setupFiles` runs per worker — loads the right env vars. Two files because they run in different processes.

(The `transformIgnorePatterns` line is there because `uuid` ships ESM-only in newer versions, and Jest needs to transform it.)

## E2e Tests Are User Flows, Not Isolated Units

One design question I had: does calling a membership endpoint inside an organization e2e test violate separation of concerns?

No — and this is an important distinction. Unit tests isolate. The org service test only tests the org service. But e2e tests verify **user flows**. A real user would:

1. Register an account
2. Create an organization
3. Check that they're the owner

That's one coherent scenario. The e2e test mirrors it:

```typescript
describe('Register a user, create an org, verify OWNER membership', () => {
    it('POST /auth/register', async () => {
        await request(app.getHttpServer())
            .post('/api/v1/auth/register')
            .send({
                email: 'org-test@test.com',
                password: 'password123',
                displayName: 'E2E Org Test'
            })
            .expect(201)
            .expect((res) => {
                accessToken = res.body.data.accessToken;
            });
    });

    it('should create org and verify OWNER membership', async () => {
        const orgRes = await request(app.getHttpServer())
            .post('/api/v1/organizations')
            .set('Authorization', `Bearer ${accessToken}`)
            .send({ name: 'Test Org', description: 'E2E testing org.' });

        expect(orgRes.statusCode).toBe(201);

        const membership = await request(app.getHttpServer())
            .get(`/api/v1/organizations/${orgRes.body.data.id}/members/find`)
            .set('Authorization', `Bearer ${accessToken}`);

        expect(membership.body.data.role).toBe('OWNER');
    });
});
```

The create endpoint only returns the org — not the membership. So verifying OWNER requires a second request to the membership endpoint. That's not a test smell. That's the test doing what a user would do.

## Takeaway

Unit tests and e2e tests aren't redundant — they catch different things. Unit tests prove your logic is correct. E2e tests prove your app actually works when a real HTTP request hits it. A missing decorator, a wrong status code, a misconfigured pipe — these are invisible to unit tests.

Set up both. Run both. Trust neither alone.
```

---

### Substack
```
Subject: The bug that unit tests will never catch

Today I set up proper e2e test infrastructure for RunHop — dedicated test database, automatic resets, the works.

And within the first run, it caught a bug.

The org update endpoint had a missing @Body() decorator. The method signature looked right. The service unit tests all passed. But when a real HTTP request hit the endpoint, the DTO was always undefined. NestJS just... never parsed the body.

Unit tests couldn't catch this because they call the service directly. They skip routing, decorators, pipes — the entire HTTP layer. The logic was correct. The wiring was broken.

It's a good reminder: unit tests prove your functions work. E2e tests prove your app works. They test different things.

Also migrated from Docker Desktop to Colima today. Same Docker engine, noticeably less resource usage. And started reading Grokking Algorithms — went through cursor pagination, which I'd already implemented in Prisma. Seeing the theory after writing the code made both click differently.

Talk soon,
Rhico
```
