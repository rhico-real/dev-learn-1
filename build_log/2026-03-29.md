# Build Log: Mar 29, 2026
**Project:** RunHop — a social + event platform for running races
**Stack:** NestJS · TypeScript · PostgreSQL · Prisma · Redis · Jest · Colima
**Task:** 6 — Organization Context (completing e2e tests + pagination)

---

## What I Did

Finished Task 6. Completed the organization e2e test suite and wrote the full org-membership e2e suite from scratch.

On the org side:
- Wired `PaginationQueryDTO` into the organization controller — was using inline `@Query()` params before, which meant `limit` was arriving as a string and Prisma silently ignored it. The DTO's `@Type(() => Number)` handles the coercion.
- Refactored `OrganizationService.list()` to return `{ data, meta: { cursor } }` instead of a raw array — aligns with the `TransformInterceptor`'s passthrough check (`response.data && response.meta`).
- Fixed the unit test mocks: `findMany` mock was returning `{ data: [...], nextCursor }` (an object) instead of a plain array. `.at(-1)` blew up because objects don't have array methods.
- Three e2e pagination tests: first page (no cursor), next page (with cursor, verified no overlap), and empty result when cursor is past the last item.

On the org-membership side — 6 e2e scenarios, all from the plan:
- Add a member (201)
- Duplicate member (409)
- Update role as OWNER (200)
- Update role as ADMIN (403)
- Remove a member (200)
- Remove OWNER (403)

Setup: three users (owner, admin, member), one org. Each test builds on the state from previous tests — sequential by design, since membership operations are stateful.

---

## Struggles

**Mock shape vs real Prisma return types.** The `findMany` mock was returning an object with `data` and `nextCursor` keys — mimicking what the API response looks like, not what Prisma actually returns. Prisma's `findMany` returns a plain array. The `{ data, meta }` wrapping happens in the `TransformInterceptor`, which lives at the HTTP layer — the service never sees it. Took a minute to trace the data flow: Prisma → service → controller → interceptor → HTTP response.

**Overlap check logic in pagination test.** First version built a Set from `newList` and then checked `newList.every(item => set.has(item.id))` — which is comparing a list against itself. Always true. Fixed to check `listOrgs.every(item => page2Ids.has(item.id))` — verifying that page 1 items don't appear in page 2.

**Query param naming mismatch.** The `PaginationQueryDTO` uses `limit`, but the service parameter is called `take`. The controller maps between them (`dto.limit` → service's `take` param). Tests kept using `take=5` in the URL, which was silently ignored because the DTO has no `take` field.

---

## Wins

- Task 6 e2e coverage is complete — both organization and org-membership suites pass.
- The `{ data, meta }` response shape is consistent now. Service returns the paginated structure, interceptor detects it and passes through untouched, non-paginated responses still get wrapped in `{ data }` automatically.
- The org-membership test suite is clean — three users with distinct roles, each test exercises a specific permission boundary.
- Unit test mocks now match actual Prisma return types, not API response shapes. Should prevent the same class of bug going forward.

---

## Next Up

- Task 7 (Event Context)
- Continue Grokking Algorithms

---

## STAR Story (Apple Behavioral)

**Situation:** The organization service's `list()` method was returning a raw Prisma array, but the API needed cursor-based pagination with a consistent `{ data, meta }` response shape that worked with the existing global `TransformInterceptor`.

**Task:** Implement cursor pagination in the service layer and ensure the response flows correctly through the interceptor — without breaking the existing response wrapping for non-paginated endpoints.

**Action:** Refactored `list()` to call `findMany`, extract the last item's ID as `nextCursor` via `result.at(-1)?.id`, and return `{ data: result, meta: { cursor: nextCursor } }`. The interceptor already had a passthrough check: if a response has both `data` and `meta` keys, it skips wrapping. Wired `PaginationQueryDTO` (with `@Type(() => Number)` for query param coercion) into the controller. Fixed the unit test mocks to return plain arrays instead of objects, matching real Prisma behavior. Wrote three e2e tests covering first page, cursor continuation, and end-of-list.

**Result:** Pagination works end-to-end. The `{ data, meta }` shape is consistent across paginated and non-paginated endpoints — paginated responses pass through untouched, everything else gets wrapped automatically. The `PaginationQueryDTO` is reusable for every future list endpoint (events, members, etc.). All 9 organization e2e tests and 6 membership e2e tests pass.

---

## Social Posts

### X (Twitter)
```
day 12 of building RunHop

today: finished Task 6 — org + membership e2e suites
→ cursor-based pagination with { data, meta } response shape
→ 6 membership e2e tests: add, duplicate 409, role updates, permission 403s
→ traced a .at(-1) crash to a mock returning an object instead of an array

mock what Prisma returns, not what your API returns. different layers, different shapes.
```

---

### LinkedIn
```
Day 12 building RunHop — a social + event platform for running races on NestJS.

Today: completed Task 6 with full e2e coverage for organizations and org-membership.

→ Cursor-based pagination in the org service — { data, meta: { cursor } } response shape
→ Wired PaginationQueryDTO with @Type(() => Number) for query param coercion
→ 6 org-membership e2e tests covering add, duplicate (409), role updates, and permission boundaries (403)
→ Fixed unit test mocks that were returning API response shapes instead of raw Prisma return types

The interesting debugging session: the org service called result.at(-1) on what it expected to be an array. The mock was returning { data: [...], nextCursor: '...' } — mimicking the API response, not what Prisma's findMany actually returns. Objects don't have .at(). The fix was one line, but the lesson matters: mock the layer you're testing, not the layer above it.

The response flow is clean now. Services return { data, meta } for paginated endpoints. The global TransformInterceptor detects both keys and passes through untouched. Non-paginated responses still get wrapped in { data } automatically. One interceptor, two behaviors, zero special cases per endpoint.

Next: Task 7 — Event Context.

#NestJS #TypeScript #Testing #CursorPagination #SoftwareArchitecture #LearningInPublic
```

---

### dev.to
```
---
title: "Mock What Prisma Returns, Not What Your API Returns"
tags: nestjs, typescript, testing, learninpublic
---

I'm building RunHop in public — a social + event platform for running races, built on NestJS. This is day 12.

Today I finished the organization and membership e2e test suites. Along the way, I spent an embarrassing amount of time on a TypeError that came down to one wrong mock.

## The Crash

```typescript
// organization.service.ts
async list(cursor?: string, take: number = 20) {
    const args: Prisma.OrganizationFindManyArgs = {
        take,
        where: { deletedAt: null },
        orderBy: { createdAt: 'desc' }
    };

    if (cursor) {
        args.skip = 1;
        args.cursor = { id: cursor }
    }

    const result = await this.prisma.organization.findMany(args);
    const nextCursor = result.at(-1)?.id;

    return { data: result, meta: { cursor: nextCursor } };
}
```

The unit test:

```typescript
it('should return orgs without cursor', async () => {
    mockPrisma.organization.findMany.mockResolvedValue(
        { data: [mockOrg], nextCursor: mockOrg.id }  // <-- the problem
    );
    const result = await service.list();
    // TypeError: result.at is not a function
});
```

`result.at(-1)` crashed because the mock returns an object, not an array. And `.at()` is an Array method.

## Why This Happens

The mock was shaped like the API response — `{ data: [...], nextCursor: '...' }`. That's what the frontend sees. But Prisma's `findMany` returns a plain array. Always. The `{ data, meta }` wrapping happens later, in a completely different layer.

Here's the actual data flow in a NestJS app with a response interceptor:

```
Prisma findMany  →  returns [org1, org2, org3]        (plain array)
     ↓
Service          →  returns { data: [...], meta: {} }  (structured response)
     ↓
Controller       →  returns service result as-is
     ↓
TransformInterceptor  →  sees data + meta keys → passes through untouched
     ↓
HTTP Response    →  { data: [...], meta: { cursor: '...' } }
```

For non-paginated endpoints, the interceptor wraps automatically:

```
Service          →  returns { id: '123', name: 'Org' }  (plain object)
     ↓
TransformInterceptor  →  no data/meta keys → wraps in { data: response }
     ↓
HTTP Response    →  { data: { id: '123', name: 'Org' } }
```

The interceptor code is straightforward:

```typescript
// transform.interceptor.ts
@Injectable()
export class TransformInterceptor<T> implements NestInterceptor<T, any> {
    intercept(context: ExecutionContext, next: CallHandler): Observable<any> {
        return next.handle().pipe(
            map((response) => {
                if (!response) return { data: null };
                if (response.data && response.meta) return response;
                return { data: response };
            })
        );
    }
}
```

When you write a unit test for the service, you're testing everything above the controller. The mock replaces Prisma — so it should return what Prisma returns:

```typescript
mockPrisma.organization.findMany.mockResolvedValue([mockOrg]);  // plain array
```

## The Pagination Test That Compared a List to Itself

While I was at it, I wrote a pagination e2e test that verified page 2 doesn't overlap with page 1. First attempt:

```typescript
const newList = result.body.data;
const bIds = new Set(newList.map((item) => item.id));
const same = listOrgs.length === newList.length
    && newList.every((item) => bIds.has(item.id));

expect(same).toBe(false);
```

This always passes. `bIds` is built from `newList`, and then we check if every item in `newList` is in `bIds`. Of course it is — we're comparing the list against itself.

The fix:

```typescript
const page2Ids = new Set(newList.map((item) => item.id));
const hasOverlap = listOrgs.some((item) => page2Ids.has(item.id));

expect(hasOverlap).toBe(false);
```

Now we're checking: do any page 1 items appear in page 2? That's the actual invariant.

## Membership E2E: Permission Boundaries

The org-membership suite tests 6 scenarios from the plan:

```typescript
describe('Add a member', () => {
    it('should create and return org membership');         // 201
    it('should reject duplicate member');                   // 409
});

describe('Update Member', () => {
    it('should update member role as owner');               // 200
    it('should reject role update as admin');               // 403
});

describe('Remove member', () => {
    it('should remove a member');                           // 200
    it('should reject removing owner');                     // 403
});
```

Setup is three users with distinct roles: owner creates the org (gets OWNER automatically via the transaction in `create()`), admin gets added with ADMIN role, member gets added with default MEMBER role. Tests run sequentially because membership state carries between them — the admin added in the update suite's `beforeAll` is still there for the remove tests.

The permission model: the controller calls `verifyRole(userId, orgId, minRole)` before every mutation. The service checks the caller's role against a hierarchy map:

```typescript
const ROLE_HIERARCHY: Record<string, number> = {
    MEMBER: 1,
    ADMIN: 2,
    OWNER: 3
};
```

OWNER can do everything. ADMIN can add and remove members but not change roles. MEMBER can't mutate anything.

## Takeaway

Mock the layer you're replacing, not the layer you're consuming. If you're mocking Prisma, return what Prisma returns. If you're mocking a service, return what the service returns. The moment your mock matches a different layer's shape, your test is lying to you — and you'll chase TypeErrors that have nothing to do with your actual logic.
```

---

### Substack
```
Subject: The mock that lied about its shape

Spent the day finishing RunHop's organization and membership e2e tests. 15 tests across two suites, all passing.

The debugging session that stuck with me: a `TypeError: result.at is not a function` in the org service. The mock for `findMany` was returning `{ data: [...], nextCursor: '...' }` — shaped like the API response. But Prisma's `findMany` returns a plain array. The `{ data, meta }` wrapping happens in the interceptor, a completely different layer.

One-line fix. But the principle is worth remembering: mock the layer you're replacing, not the layer above it. When a mock's shape doesn't match the real return type, your test isn't testing what you think it's testing.

Also wrote my first cursor pagination tests. The trick: fetch page 1, grab `meta.cursor` from the response, pass it as a query param for page 2, verify zero overlap. Simple once you see it, but I initially wrote an overlap check that compared a list to itself. Always passed. Always wrong.

Talk soon,
Rhico
```
