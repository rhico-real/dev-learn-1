# Build Log: Mar 18 – 23, 2026
**Project:** RunHop — a social + event platform for running races
**Stack:** NestJS · TypeScript · PostgreSQL · Prisma · Redis · JWT
**Tasks:** 1–5 (Setup → Auth → User → Shared infrastructure → Tests)

---

## What I Did

### Task 1 — Architecture & Setup (Mar 18)
Started with architecture first, code second. Went with a bounded-context / domain-driven folder structure (`src/domain/identity/`, `src/domain/organization/`, etc.) — the kind of structure that holds up as the app grows instead of becoming a mess at scale.

Designed the Prisma schema upfront: `User`, `Organization`, `OrgMembership`, `Event`, `EventRegistration`. Set up Docker Compose with PostgreSQL and Redis.

### Task 2 — Prisma + Database (Mar 20)
Defined the full schema, ran first migrations, wired up `PrismaService` and `RedisService` into the NestJS app.

### Task 3 — Auth Module (Mar 20–21)
Built the full auth flow — register, login, refresh token, logout. The important part: logout actually *works*. JWTs are stateless so you can't cancel them server-side by default. The fix is storing refresh tokens in Redis and deleting them on logout, so a revoked token can never be reused.

Set up `JwtStrategy` with Passport for protected route validation.

### Task 4 — User Module (Mar 21)
`GET /users/me`, `PATCH /users/me`, `DELETE /users/me`. Used a `@CurrentUser()` decorator to pull the authenticated user from the JWT payload — cleaner than passing IDs manually through every controller method.

### Task 5 — Shared Infrastructure + Tests (Mar 22–23)
Built `SharedModule` — `JwtAuthGuard`, `RolesGuard`, `TransformInterceptor`, `LoggingInterceptor`, `HttpExceptionFilter`. Then wrote unit tests for services and guards, plus E2E tests covering the full auth flow.

Hit a real bug in the E2E tests: the logout test was passing when it shouldn't. Turned out the Redis write (storing the invalidated token) wasn't being awaited before the next request fired. The test was essentially racing itself. Fixed it — the test now actually proves what it's supposed to prove.

---

## Struggles

**TypeScript's decorator patterns in NestJS** — coming from Dart, the syntax is close but NestJS leans heavily on decorators in ways that aren't immediately intuitive. Took a few repetitions before it stopped feeling like magic.

**NestJS DI vs just importing modules** — the module/provider declaration pattern is more verbose than I'm used to. The benefit is clear (explicit dependencies, testability) but the mental overhead is real until it clicks.

**The async race condition** in the E2E test was the most interesting one. The test was structured correctly but the timing was wrong. A missing `await` on the Redis call was the entire problem.

---

## Wins

- Solid auth system with refresh token rotation and logout invalidation. Not the shortcut version.
- E2E test coverage on the full auth flow, including the edge case of using an invalidated token.
- The bounded-context structure is already paying off — every new file has an obvious home.
- Understood the real tradeoff between stateless JWTs and server-side session control.

---

## Next Up
Task 6 — Organization Context.

---

## STAR Story (Apple Behavioral)

**Situation:** Starting a new backend project in a stack I hadn't used before (NestJS/TypeScript), with the goal of building production-grade patterns from day one — not just getting things to work.

**Task:** Stand up the full auth system: JWT with refresh token rotation, Redis-based logout invalidation, role-based guards, and test coverage including E2E.

**Action:** Designed the architecture before writing code. Applied domain-driven structure to keep the codebase maintainable at scale. Built proper token revocation using Redis instead of the typical "just let the token expire" approach. Caught and fixed a real async race condition in the E2E test suite.

**Result:** Working, tested auth system in under a week — register, login, refresh, logout, role guards, E2E coverage. The foundation is solid enough that every feature I add on top of it is faster to build.

---

## Social Posts

### X (Twitter)
```
been building RunHop — a social platform for running races — as a way to go deep on NestJS

week 1:
→ domain-driven folder structure from day 1
→ JWT auth with Redis-based token revocation (real logout, not fake logout)
→ unit + E2E tests
→ caught a fun async race condition in the test suite

stateless auth isn't actually stateless if you want logout to mean something
```

---

### LinkedIn
```
I've been building RunHop — a social + event platform for running races — to go deep on the NestJS/TypeScript ecosystem.

Week 1 recap (Tasks 1–5):

→ Designed the full system architecture before writing code — bounded-context / DDD folder structure built for scale, not just "it works today"
→ Set up NestJS with PostgreSQL, Prisma, and Redis
→ Built full JWT auth with refresh token rotation and Redis-based logout invalidation — because stateless tokens can't be revoked unless you track them somewhere
→ Wrote unit and E2E tests, including catching an async race condition where a Redis write wasn't awaited before the next HTTP request fired

The interesting part isn't any single feature — it's how the architecture decisions made in day 1 either pay off or cost you later. Going DDD from the start is already compounding.

More updates as the project grows.

#NestJS #TypeScript #SoftwareEngineering #LearningInPublic #SystemDesign
```

---

### dev.to
```
---
title: "Week 1 Building RunHop: DDD Structure, JWT Auth, and a Race Condition I Didn't See Coming"
tags: nestjs, typescript, architecture, learninpublic
---

I'm building RunHop — a social platform for running races — as a way to go deep on the NestJS/TypeScript stack. This is week 1.

The goal from day 1 was to build it the way a real production system gets built: domain-driven structure, proper auth, test coverage from the start. Not a tutorial app.

Here's what the first week looked like.

## Architecture First

Before writing a single line of app code, I mapped out the domain structure. RunHop has clear bounded contexts — `identity` (auth, users), `organization` (clubs, membership), `event` (races, registrations) — so the folder structure reflects that:

```
src/
  domain/
    identity/
    organization/
    event/
  shared/
```

Each context gets its own module, services, controllers, and DTOs. No flat `src/services/` folder that becomes a dumping ground by month two.

The Prisma schema covers the full data model upfront: `User`, `Organization`, `OrgMembership`, `Event`, `EventRegistration`. Designing it early forced me to think through the relationships before writing business logic — which fields are required, where the foreign keys go, what cascades on delete.

This matters at scale. When every feature you add has to figure out where it lives, you lose time on decisions that should already be made. DDD structure pays for itself fast.

## The Logout Problem Everyone Ignores

JWTs are stateless — which sounds great until you realize it means you can't actually log someone out. Once a token is issued, it's valid until it expires. There's no server-side session to destroy.

Most tutorials skip this entirely. The token expires in 15 minutes, so who cares? In production, you care. Users expect logout to mean logout.

The fix: store refresh tokens in Redis. On login, the refresh token gets written to Redis with a TTL matching its expiration. On logout, delete it. When the client tries to use the refresh token to get a new access token, it's gone.

```typescript
async logout(userId: string): Promise<void> {
  await this.redis.del(`refresh_token:${userId}`);
}

async refreshTokens(userId: string, refreshToken: string) {
  const stored = await this.redis.get(`refresh_token:${userId}`);
  if (!stored || stored !== refreshToken) {
    throw new UnauthorizedException('Invalid refresh token');
  }
  // Issue new token pair, rotate refresh token
}
```

Simple in concept. The access token still lives until it expires (short TTL), but the refresh token — the one that matters for persistence — is revocable.

## The Race Condition

My E2E test was passing. It shouldn't have been.

The test flow: register → login → logout → try to use the refresh token → expect 401. Straightforward. But the logout step wasn't fully completing before the next request fired.

The problem was a missing `await` on the Redis `del()` call inside the logout handler. The function returned before Redis confirmed the deletion. Most of the time, Redis is fast enough that it didn't matter — the token was gone by the time the next request arrived. But "most of the time" isn't a test you can trust.

Once I added the `await`, the test became deterministic. It fails when it should fail.

Async timing bugs are the worst kind of test problem because they produce tests that pass — just not for the right reasons. If your test involves any async side effect, make sure every step is fully resolved before asserting.

## What's Next

Week 2 is the Organization domain — multi-tenant orgs with role-based membership (OWNER, ADMIN, MEMBER). The bounded-context structure should make this straightforward.

This is part of a build-in-public series. I'll be posting updates as the project grows.
```

---

### Substack
```
Subject: Week 1 — Architecture decisions, real auth, and a sneaky test bug

Building RunHop in public. Week 1 is done.

The stack is NestJS, PostgreSQL, Redis, and Prisma. The goal is to build it properly — not to get something working fast and fix the mess later.

Week 1 was mostly foundation work: architecture design, database setup, full auth system, and tests. Not glamorous but it's the stuff that determines whether the codebase is still maintainable in three months.

The most interesting thing this week was the auth system. JWT-based auth is everywhere, but most implementations skip a real logout mechanism. Stateless tokens can't be invalidated by default — once issued, they're valid until they expire. The solution is Redis: store refresh tokens server-side, delete them on logout. Simple in concept, easy to get wrong in execution.

I got it wrong once. My E2E test for logout was passing when it shouldn't have been. Took me longer than I'd like to admit to find the problem: a Redis write wasn't being awaited, so the invalidation hadn't happened yet when the next request fired. Classic async timing bug, hidden by the fact that the test *usually* passes fast enough that it doesn't matter.

Fixed it. The test is honest now.

Week 2 is the Organization domain — multi-tenant orgs with role-based membership.

Talk soon,
Rhico
```
