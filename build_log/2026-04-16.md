# Build Log: Apr 16, 2026
**Project:** RunHop — a social + event platform for running races
**Stack:** NestJS · TypeScript · PostgreSQL · Prisma · Redis · Jest · Docker
**Task:** 4 — Reactions (likes on posts)

---

## What I Did

Built the first pass of the Reactions module for post likes. This adds the service, controller, unit tests, e2e coverage, and module wiring so likes now sit properly inside the Social context instead of being just a schema placeholder.

- Added `ReactionService` in `src/domain/social/reaction/reaction.service.ts` (81 lines). It has `like`, `unlike`, and `ownershipCheck`.
- `like(postId, userId)` verifies the post exists through `PostService.findById()`, then creates a `postLike` row.
- Duplicate likes are translated from Prisma `P2002` into `ConflictException('Duplicate like')`.
- `unlike(likeId)` deletes a like row and translates Prisma `P2025` into `NotFoundException('Like record does not exist')`.
- `ownershipCheck(likeId, userId)` reads the `postLike` record first and blocks deletes when the like belongs to someone else.
- Added `ReactionController` in `src/domain/social/reaction/reaction.controller.ts` (26 lines) with:
  - `POST /posts/:id/likes`
  - `DELETE /posts/:id/likes`
- The delete route currently treats `:id` as a like record id, so the path shape stays under `/posts` even though the actual delete target is the `PostLike` row.
- Wired `ReactionModule` in `src/domain/social/reaction/reaction.module.ts` to import `IdentityContextModule` and `PostModule`, then registered the controller and service.
- Added unit tests in `src/domain/social/reaction/reaction.service.spec.ts` (111 lines) for:
  - successful like
  - liking a missing post
  - duplicate like conflict
  - successful unlike
  - unliking a missing like record
- Added e2e coverage in `test/e2e/reactions.e2e-spec.ts` (93 lines) for:
  - liking a post
  - duplicate like returning `409`
  - unliking the created like
  - unliking the same record again returning `404`
- Regenerated `docs/erd.svg` so the schema docs include the social tables and relationships as they exist now.

---

## Struggles

**Async Prisma errors inside `try/catch`.** The duplicate-like test looked correct at first, but the service was returning `this.prisma.postLike.create(...)` directly inside the `try` block. That means the method returned the promise before the rejection happened, so the `catch` block never saw the Prisma `P2002`. The fix was `return await this.prisma.postLike.create(...)` inside the `try`. This is one of the rare places where `return await` is the correct move.

**Route shape for unlike is a little awkward.** The create route naturally keys off the post id: `POST /posts/:id/likes`. The unlike route was kept under the same controller as `DELETE /posts/:id/likes`, but at delete time the service actually needs the like id, not the post id. Functionally it works with the current e2e test because the URL segment is the `reactionRecord.id`, but semantically the route name is carrying two different meanings. It’s workable for now, but worth revisiting before the API gets wider usage.

**Ownership checks are split between controller and service.** `ReactionService.unlike()` only handles the delete and Prisma not-found mapping. Ownership validation happens one layer higher through `ownershipCheck()` in the controller first. That keeps the service methods small, but it also means delete correctness depends on both methods being called in the right order.

---

## Wins

- The `PostLike` relation already in the schema dropped cleanly into the service layer once the module scaffolding was in place.
- Reusing the same exception mapping pattern from `FollowService` made duplicate-like handling straightforward.
- The e2e path is short and useful: register user → create post → like → duplicate like → unlike → unlike again. It gives quick confidence without needing a huge fixture setup.
- Catching the async `try/catch` issue now is useful because the same mistake shows up easily in Prisma service code when handling constraint errors.

---

## Next Up

- Decide whether unlike should stay as `DELETE /posts/:id/likes` with `:id` meaning like id, or switch to a less ambiguous route shape.
- Add ownership-focused tests for deleting someone else’s like.
- Start on the next social feature after reactions are stable.

---

## STAR Story (Apple Behavioral)

**Situation:** I was wiring the Reactions module for post likes and wrote a unit test for duplicate likes expecting Prisma `P2002` to turn into a Nest `409 Conflict`.

**Task:** Make the service behave like the Follow module: catch the database uniqueness violation and return an application-level exception instead of leaking Prisma details.

**Action:** I traced the flow from the failing unit test into `ReactionService.like()`. The logic looked right until I noticed the Prisma call was returned directly from inside a `try` block without `await`. That meant the promise rejection happened after control had already left the `try/catch`. I changed it to `return await this.prisma.postLike.create(...)`, kept the explicit `P2002` mapping, and mirrored the same pattern for `unlike()` with `P2025`.

**Result:** The duplicate-like path now maps cleanly to `ConflictException`, the delete path maps missing records to `NotFoundException`, and the module behavior is consistent with the rest of the codebase’s Prisma error handling.

---

## Social Posts

### X
```text
Day 25 of RunHop: built the Reactions module in NestJS.

Added post likes with:
-> `POST /posts/:id/likes`
-> `DELETE /posts/:id/likes`
-> Prisma P2002 -> 409 Conflict
-> Prisma P2025 -> 404 Not Found
-> unit tests + e2e flow

The useful catch today: `try/catch` does not help if you return a rejected promise without `await`.
```

### LinkedIn
```text
Day 25 building RunHop — a social + event platform for running races.

Today I added the Reactions module for post likes in NestJS.

What shipped:
- `ReactionService` with `like`, `unlike`, and `ownershipCheck`
- `ReactionController` with `POST /posts/:id/likes` and `DELETE /posts/:id/likes`
- Prisma error mapping:
  - `P2002` -> `409 Conflict` for duplicate likes
  - `P2025` -> `404 Not Found` for missing like records
- unit tests for service behavior
- e2e coverage for like, duplicate like, unlike, and unlike-again

The most useful bug today was an async one. I had a Prisma create call inside a `try/catch`, but I returned the promise directly instead of awaiting it. That meant the rejection happened outside the `catch`, so the duplicate-like path leaked the Prisma error instead of becoming a Nest exception.

Small detail, real impact. In async service code, `return await` is usually unnecessary, except when you need the `catch` block to actually see the rejection.

Next I want to clean up the unlike route shape because the current delete endpoint uses a post-looking path while actually deleting by like id.

#NestJS #TypeScript #Prisma #Testing #BackendDevelopment #LearningInPublic
```

### dev.to
```text
---
title: "Why My Prisma P2002 Was Escaping try/catch in NestJS"
tags: nestjs, typescript, prisma, learninpublic
---

I'm building RunHop in public, a social + event platform for running races. Today I worked on the Reactions module: likes on posts.

The module itself was straightforward:

- `POST /posts/:id/likes`
- `DELETE /posts/:id/likes`
- a `ReactionService` that talks to Prisma's `postLike` model
- unit tests and e2e coverage for the happy path and duplicate-like path

The interesting bug was in the duplicate-like flow.

## The Service Looked Correct

I had this shape in `src/domain/social/reaction/reaction.service.ts`:

```ts
async like(postId: string, userId: string) {
    const post = await this.postService.findById(postId);

    if (!post) throw new NotFoundException('Post not found');

    try {
        return this.prisma.postLike.create({
            data: { postId, userId },
        });
    } catch (error) {
        if (
            error instanceof PrismaClientKnownRequestError &&
            error.code === 'P2002'
        ) {
            throw new ConflictException('Duplicate like');
        }

        throw error;
    }
}
```

At a glance it feels fine. There's a `try/catch`, Prisma throws `P2002` for a unique constraint violation, and I map it to `ConflictException`.

But the unit test still failed.

## Why It Failed

`postLike.create()` returns a promise. When that promise rejects, the rejection happens asynchronously.

Because I returned the promise directly from inside the `try` block, the function exited before the rejection occurred. The `catch` block never saw the Prisma error.

So instead of this:

- Prisma rejects with `P2002`
- service catches it
- Nest gets `ConflictException`

I got this:

- Prisma rejects with `P2002`
- promise escapes
- raw Prisma error bubbles up

## The Fix

The fix was to await inside the `try` block:

```ts
try {
    return await this.prisma.postLike.create({
        data: { postId, userId },
    });
} catch (error) {
    if (
        error instanceof PrismaClientKnownRequestError &&
        error.code === 'P2002'
    ) {
        throw new ConflictException('Duplicate like');
    }

    throw error;
}
```

This is one of the few cases where `return await` is exactly what you want. It keeps the rejected promise inside the `try/catch` boundary.

## The Rest of the Module

I also added:

- `ownershipCheck(likeId, userId)` so only the owner can delete a like
- `unlike()` mapping Prisma `P2025` to `NotFoundException`
- an e2e test that does the full flow:
  - register
  - create post
  - like
  - duplicate like -> `409`
  - unlike
  - unlike again -> `404`

One design detail I still want to revisit: the delete route is `DELETE /posts/:id/likes`, but the `:id` there is currently a like id, not a post id. It works, but the route shape is carrying two meanings between create and delete.

## Takeaway

The useful reminder from this session:

`try/catch` only catches what stays inside it.

If you need to translate async database errors into framework exceptions, you need the rejected promise to be awaited inside the `try` block.
```

### Substack
```text
Subject: The tiny async mistake that let Prisma errors escape

Today’s work was the Reactions module for RunHop — basically likes on posts.

The code wasn’t especially large, but it had one good bug in it. I had a Prisma create call inside a `try/catch`, and I assumed that was enough to catch duplicate-like errors. It wasn’t. I returned the promise directly, so the rejection happened after the function had already left the `try` block.

The fix was small: `return await ...` inside the `try`.

That one detail made the difference between leaking a Prisma `P2002` and returning a clean `409 Conflict`.

It’s the kind of bug that’s easy to miss because the code looks right when you read it quickly.

Talk soon, Rhico
```
